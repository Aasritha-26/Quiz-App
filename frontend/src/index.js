import React from 'react';
import ReactDOM from 'react-dom/client';
import './App.css';  // Optional if you're using app.css instead
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
